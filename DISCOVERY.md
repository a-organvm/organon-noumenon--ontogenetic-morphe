# Discovery

organvm/organon-noumenon--ontogenetic-morphe has real ranked value as the estate's reusable symbolic-process kernel: it is not only a theory archive, but a Python package (`autogenrec`) with typed symbolic values and provenance, a recursive four-phase `ProcessLoop`, an async wildcard `MessageBus`, a lifecycle `Orchestrator`, 22 concrete subsystem implementations, subsystem specs, integration tests, examples, CI, and a pitch surface. Its highest latent value is to become the importable I -> II -> III contract for downstream organs that need local symbolic routing, identity/audience modeling, process monetization, academic/archive flows, conflict arbitration, and signal thresholding without pulling in vendor agent frameworks. First task: harden the public SDK surface and smoke gate by fixing the README imports to the real modules, documenting the minimal supported imports (`SymbolicValue`, `ProcessLoop`, `MessageBus`, `create_default_orchestrator`, and the value/identity subsystems), and adding a CI smoke test that imports the package, starts the default orchestrator, and runs the value-exchange demo path.
